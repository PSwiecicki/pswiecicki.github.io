import styled from 'styled-components';

export const Wrapper = styled.section`
  max-width: 996px;
  margin: auto;

  div {
    display: flex;
    align-items: center;
  }

  p {
    margin: 0;
  }
`;
